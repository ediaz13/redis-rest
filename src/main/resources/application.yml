spring:
  application:
    name: @project.name@
    description: @project.description@
    version: @project.version@
    
  redis:
    host: localhost
    jedis:
      pool:
        max-active: 16
        max-idle: 8
        min-idle: 4
    password: ''
    port: 6379
  session:
    redis:
      #flush-mode: 'on-save '
      namespace: 'spring:session '
      store-type: redis

info:
  app:
    name: @project.name@
    description: @project.description@
    version: @project.version@

jwt:
  secret: s3cr3t
  expiration: 10
   
server:
  servlet:
  session:
  timeout: 10